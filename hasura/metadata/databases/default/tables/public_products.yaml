table:
  name: products
  schema: public
select_permissions:
  - role: customer
    permission:
      columns: [id, sku, name, description, unit_price_cents, stock_qty]
      filter: {}
  - role: ops
    permission:
      columns: [id, sku, name, description, unit_price_cents, stock_qty, created_at]
      filter: {}
  - role: admin
    permission:
      columns: [id, sku, name, description, unit_price_cents, stock_qty, created_at]
      filter: {}
