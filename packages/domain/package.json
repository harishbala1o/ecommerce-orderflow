{
  "name": "@orderflow/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  }
}
